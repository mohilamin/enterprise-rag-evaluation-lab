# Screenshot: Search Lab

Capture the Streamlit "Search Lab" section using this example query:

```text
What controls are required for privileged access?
```

What to capture:

- Search input field.
- Ranked retrieval results.
- Document IDs, chunk IDs, titles, scores, and snippets if visible.

Why it matters:

This shows that the project evaluates retrieval quality directly, which is the foundation of trustworthy RAG.

Suggested file name: `search-lab.png`
