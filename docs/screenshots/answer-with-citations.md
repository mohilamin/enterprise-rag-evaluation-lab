# Screenshot: Answer With Citations

Capture the Streamlit "Example Answer With Citations" section using:

```text
How quickly must vendors report a confirmed data incident?
```

What to capture:

- Question input.
- `answer_text`.
- `citations` with `document_id` and `chunk_id`.
- Confidence, groundedness, citation coverage, and risk warning fields.

Why it matters:

This demonstrates the difference between a plain chatbot answer and a cited, auditable answer.

Suggested file name: `answer-with-citations.png`
