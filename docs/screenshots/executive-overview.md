# Screenshot: Executive Overview

Capture the top of the Streamlit dashboard after running:

```bash
python -m src.pipeline.run_all
python -m streamlit run src/dashboard/app.py
```

What to capture:

- Page title.
- KPI cards for RAG Trust Score, Hit@3, Answerability, and Citation Coverage.
- The short explanation under the KPI cards.

Why it matters:

This screenshot gives recruiters and hiring managers the fastest proof that the project has business-facing evaluation outputs, not just code.

Suggested file name: `executive-overview.png`
